openai>=1.0.0
pandas>=1.3.0
numpy>=1.20.0
matplotlib>=3.4.0
seaborn>=0.11.0
tqdm>=4.60.0
argparse>=1.4.0 

# Optional dependencies
# =====================
# The benchmark will run without these, but with reduced functionality.

# AILuminate SUT integration (optional)
# If not installed, the benchmark will run in compatibility mode using our standard benchmark.
# To enable full AILuminate SUT integration, uncomment the line below:
# modelgauge>=0.1.0 